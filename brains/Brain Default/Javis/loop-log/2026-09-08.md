
## [2026-09-08 00:17] dang-royce-shop-ngau-nhien-moi-5-phut · loop (custom/full) - manual
Mình sẽ chạy đúng một vòng đăng bài theo fast path: nạp skill đăng Facebook, kiểm tra dữ liệu brand/dataset cần thiết, chọn khóa học không trùng vòng trước nếu có dấu vết, rồi đăng thật nếu tool Facebook khả dụng.
Mình đã xác nhận skill yêu cầu chỉ đọc một brand kit và dùng `fb_page_album` cho bài khóa học. Tiếp theo mình đọc kit `royce-shop`, skill viết caption và script gọi hub để biết đúng tham số tool hiện có.
Kit `royce-shop` có Page ID thật và footer riêng. Mình chưa thấy log hôm nay theo UTC, nên sẽ kiểm tra state/log còn lại để tránh chọn trùng khóa ngay vòng trước.
Vòng cũ gần nhất trong log là Kế toán và AutoCAD/Đồ họa/Tin học đã từng đăng, nhưng state hiện tại của loop mới chưa ghi khóa gần nhất. Mình sẽ chọn một khóa hợp lệ ngẫu nhiên ở vòng này, tránh dựa vào post_id cũ làm khóa hiện tại.
Các folder ngành đều còn ảnh đủ để tạo album 5-8 ảnh. Mình sẽ dùng GPT Image qua `javis_generate_image`, lưu vào `_xuat`, sau đó chuẩn hóa album và đăng đúng một lần.
MCP hub hiện có `javis_generate_image` và `fb_page_album`, đủ để làm thật trong lượt này. Mình sẽ tạo payload riêng trong `scratch/` để gọi tool rõ ràng và giữ dấu vết đầu vào đã đăng.
Khóa được chọn ngẫu nhiên cho vòng này là **Kế toán thực hành**. Mình sẽ tạo poster GPT Image mới cho đúng khóa, không dùng lại cover cũ trong `_xuat`.
Lệnh tạo ảnh đang chạy qua hub, đây là bước lâu nhất vì GPT Image phải render poster hoàn chỉnh. Mình chờ kết quả để lấy path thật rồi mới chọn album và đăng.
Poster mới đã được tạo ở `_xuat`. Mình sẽ xem nhanh ảnh để kiểm các lỗi lớn như sai brand, có chữ Royce Shop, hoặc bố cục cũ trước khi đăng thật.
Ảnh đạt các điều kiện chính: có brand **TRUNG TÂM TIN HỌC SAO VIỆT**, đúng tiêu đề **KẾ TOÁN THỰC HÀNH**, không có chữ Royce Shop, hotline đúng và không dùng layout split-panel/navy cũ. Mình sẽ chọn ảnh dataset kế toán chưa dùng gần đây và chuẩn hóa album.
Album đã chuẩn hóa đủ **5 ảnh** gồm cover AI và 4 ảnh thật ngành kế toán. Mình sẽ ghi caption và payload đăng vào `scratch/`, rồi gọi `fb_page_album` đúng một lần.
Caption và payload đã được ghi vào `scratch/royce_ke_toan_caption_20260908.txt` và `scratch/royce_ke_toan_album_20260908.json`. Mình kiểm nhanh nội dung rồi đăng album thật lên Page ID `988656934325292`.
Payload hợp lệ và không có ký tự em dash. Lệnh `file` không có trong môi trường, nhưng ảnh đã được script chuẩn hóa từ Pillow và có đủ 5 path tồn tại, nên mình tiến hành đăng.
Facebook trả `post_id` thật, nghĩa là bài đã đăng thành công. Mình sẽ ghi lại các ảnh dataset gốc đã dùng và lưu trạng thái khóa vừa đăng để vòng sau tránh trùng liên tiếp.
Bài đã được xác minh công khai trên Facebook với link thật. Ghi `_anh-da-dung.md` bị chặn quyền file, mình sẽ kiểm tra quyền rồi cập nhật nếu có thể mà không thay đổi nội dung khác.
`_anh-da-dung.md` đang thuộc `root:root`, user `javis` không có quyền ghi nên không cập nhật được file đó trong lượt này. Mình vẫn sẽ cập nhật `Javis/loop-state.json` vì file này thuộc user hiện tại và cần để tránh trùng khóa vòng sau.
`Javis/loop-state.json` đã lưu khóa vừa đăng, `post_id` và link bài viết. Mình kiểm tra JSON lần cuối và rà các file đã thay đổi để báo cáo gọn.
Thư mục vault không phải git repo nên không có diff/status để đối chiếu. Việc chính đã xong: bài đã đăng và JSON state hợp lệ, chỉ còn hạn chế quyền ghi ở `_anh-da-dung.md` cần báo rõ.
Đã đăng thật **1 bài Kế toán thực hành** lên Fanpage **Royce Shop**.

- `POST_OK post_id=988656934325292_122131941183221350`
- Link: https://www.facebook.com/122131661457221350/posts/122131941183221350
- Album: **5 ảnh** gồm cover GPT Image mới + 4 ảnh dataset kế toán đã chuẩn hóa.
- Cover: `attachments/dataset/_xuat/javis-img-20260907-171322-de5c32.png`

File đã chạm:
- `scratch/royce_ke_toan_caption_20260908.txt`
- `scratch/royce_ke_toan_album_20260908.json`
- `Javis/loop-state.json`

Không cập nhật được `wiki/brand-kits/_anh-da-dung.md` vì file đang thuộc `root:root`, user `javis` bị `PermissionError`. Các ảnh dataset gốc đã dùng là:
`51f1b9e0...`, `d9ccd644...`, `z7313074765302...`, `685cfabe...`.

**Kiểm chứng:** ✓ Đạt: Các file liên quan tồn tại và nhất quán: state ghi đúng post_id/link, payload 5 ảnh hợp lệ, cover + ảnh album khớp Kế toán/Sao Việt, Page ID Royce đúng; hạn chế _anh-da-dung root:root là thật, không thấy dấu hiệu phá file hay vượt phạm vi.

## [2026-09-08 00:31] dang-royce-shop-ngau-nhien-moi-5-phut · loop (custom/full) - scheduled
Mình sẽ chạy đúng fast path cho một vòng: nạp skill đăng Facebook, kiểm tra chỉ mục/nội dung gần nhất để tránh trùng khóa học, rồi tìm cách dùng công cụ đăng thật hiện có thay vì tạo nháp.
Skill xác nhận phải đọc đúng 1 brand kit và đăng album bằng `fb_page_album`. Mình đang kiểm tra file brand kit Royce Shop, dataset theo ngành, và cách gọi plugin Facebook từ môi trường này.
Brand kit cho biết Page ID là `988656934325292`, footer/hotline dùng `0823 552 558 - 093 11 44 858`, và folder ngành có đủ nhóm `tin-hoc _ai`, `do-hoa`, `ve-ky-thuat`, `ke-toan`. Bước tiếp theo là xác định khóa học không trùng vòng trước và tạo đúng 1 poster mới.
State hiện tại ghi vòng trước là **Kế toán thực hành**, nên vòng này mình loại khóa đó khỏi danh sách. Mình sẽ chọn ngẫu nhiên trong 3 khóa còn lại và tiếp tục với poster mới.
Khóa được chọn là **Tin học văn phòng & Ứng dụng AI**. Mình sẽ tạo một poster GPT Image mới trong `attachments/dataset/_xuat/`, để GPT Image tự render logo/chữ/hotline theo brand kit Royce Shop.
Poster mới đã lưu ở `attachments/dataset/_xuat/javis-img-20260907-172450-a82f5d.png`. Mình sẽ kiểm nhanh bằng mắt để tránh lỗi rõ như sai brand, sai hotline hoặc chữ Royce Shop trước khi đăng thật.
Ảnh đạt các điều kiện chính: có “TRUNG TÂM TIN HỌC SAO VIỆT”, không có “Royce Shop”, tiêu đề đúng khóa, hotline đúng. Mình sẽ chuẩn hóa cover và 4 ảnh thật từ folder `tin-hoc _ai` thành album 5 ảnh trong `album_ready`, rồi đăng một lần bằng `fb_page_album`.
Album đang được xuất ra ảnh 2000x2000 để Facebook nhận ổn định hơn. Mình chờ lệnh xử lý ảnh kết thúc rồi sẽ dùng đúng danh sách file vừa sinh.
`file` không có trong môi trường shell, nhưng ảnh album đã tạo được và mình đã dựng contact sheet để kiểm nhanh. Nếu ảnh phụ lạc ngành rõ ràng mình sẽ loại trước khi đăng.
Contact sheet cho thấy cover đúng và 4 ảnh phụ đều là ảnh lớp/phòng máy, tổng 5 ảnh. Mình sẽ đăng đúng một lần bằng `fb_page_album` với Page ID `988656934325292`.
Lần gọi đăng bị chặn trước khi ra Facebook vì plugin không nhìn thấy file album_ready dù file đã tồn tại trong workspace. Mình sẽ kiểm đường dẫn/quyền file và thử lại với dạng đường dẫn mà MCP nhận được, chưa có bài nào được đăng ở lần lỗi này.
File có thật và quyền đọc bình thường, nhưng plugin vẫn không nhận ảnh đã tạo bằng shell trong `album_ready`. Để không tạo thêm hành động thừa, mình sẽ dùng thẳng cover do `javis_generate_image` tạo và 4 ảnh raw dataset trong vault cho lần gọi `fb_page_album` tiếp theo.
Lần gọi thứ hai cũng bị chặn ở bước kiểm file trước khi đăng, lần này ngay cả file do `javis_generate_image` trả về cũng không được plugin Facebook resolve. Mình sẽ kiểm cơ chế gọi hub/script cũ trong `scratch` và log gần nhất để tìm đúng dạng path mà `fb_page_album` chấp nhận.
Có script `scratch/hub_call.py` cũ đã từng đăng thành công vì nó gửi thêm header `X-Javis-Vault` khi gọi hub. Mình sẽ dùng đúng đường này: để script chọn/chuẩn hóa ảnh, ghi payload vào `scratch/`, rồi gọi `fb_page_album` qua hub với vault header.
Script đang chọn ảnh chưa dùng theo cache `_anh-da-dung.md` nếu đọc được và chuẩn hóa album theo layout Facebook. Sau khi có JSON ảnh, mình sẽ dùng nguyên danh sách đó để tránh lệch giữa payload và file thật.
Contact sheet cho thấy script chọn 1 poster cũ và 1 ảnh kế toán, không đạt “dataset đúng ngành” cho album phụ. Mình sẽ thay bằng danh sách ảnh lớp/phòng máy đúng ngành đã kiểm được, xuất lại đúng tên `fb_norm_00..04` rồi đăng qua hub.
Album phụ đã được thay bằng 4 ảnh lớp/phòng máy đúng folder `tin-hoc _ai`. Mình sẽ ghi caption và payload đăng vào `scratch/` để có dấu vết rõ ràng, rồi gọi hub đăng thật một lần.
Caption và payload đã được ghi vào `scratch/royce_tinhoc_ai_caption_20260908.txt` và `scratch/royce_tinhoc_ai_album_20260908.json`. Mình sẽ kiểm JSON/ký tự cấm thật nhanh rồi gọi đăng qua `scratch/hub_call.py run fb_page_album`.
Payload hợp lệ và 5 ảnh `fb_norm_00..04` tồn tại. Kiểm em dash bằng `rg` bị lỗi pattern nhưng file không có dấu dài trong phần mình ghi, nên mình tiến hành đăng qua hub để plugin nhận đúng vault.
Lệnh đăng thật đang chạy qua hub. Mình sẽ chỉ coi là xong khi có `post_id` thật từ Facebook, nếu tool trả lỗi thì báo lỗi cụ thể chứ không giả link.
Facebook đã trả `post_id` thật: `988656934325292_122131943493221350`. Mình sẽ kiểm bài mới qua `fb_page_posts`, rồi cập nhật state để vòng sau không đăng lại Tin học văn phòng & AI ngay lập tức.
Lệnh kiểm bài mới đang gọi lại Facebook để lấy permalink thật. Sau đó mình sẽ ghi `Javis/loop-state.json` và không đụng thêm hành động ngoài Facebook.
Bài đã xác minh công khai trên Facebook với permalink thật. Mình sẽ cập nhật `Javis/loop-state.json` bằng khóa vừa đăng, `post_id`, link và thời điểm chạy.
Mình sẽ cập nhật đúng block của loop hiện tại trong `Javis/loop-state.json`, giữ nguyên state của loop khác. Nội dung mới chỉ ghi khóa vừa đăng, post id, link và trạng thái đạt.
Đã đăng thật **1 bài Tin học văn phòng & Ứng dụng AI** lên Fanpage **Royce Shop**.

`POST_OK post_id=988656934325292_122131943493221350`  
Link: https://www.facebook.com/122131661457221350/posts/122131943493221350

Đã chạm file:
- `attachments/dataset/_xuat/javis-img-20260907-172450-a82f5d.png`
- `attachments/dataset/_xuat/album_ready/fb_norm_00.jpg` đến `fb_norm_04.jpg`
- `scratch/royce_tinhoc_ai_caption_20260908.txt`
- `scratch/royce_tinhoc_ai_album_20260908.json`
- `Javis/loop-state.json`

Đã xác minh bài công khai qua Facebook. Không cập nhật được `wiki/brand-kits/_anh-da-dung.md` vì file đang thuộc `root:root`, user hiện tại không có quyền ghi.

**Kiểm chứng:** ✓ Đạt: Đã xác minh post_id/permalink thật qua fb_page_posts; payload/state khớp Royce Shop, album 5 ảnh đúng khóa Tin học AI, không thấy file bị hỏng hay thao tác ngoài phạm vi.

## [2026-09-08 00:43] dang-royce-shop-ngau-nhien-moi-5-phut · loop (custom/full) - scheduled
Mình sẽ chạy đúng một vòng theo fast path: nạp skill đăng Facebook, kiểm tra cấu trúc vault và tìm tool/plugin hiện có để đăng thật lên `royce-shop`. Sau đó mình sẽ chỉ chạm các file cần thiết cho trạng thái vòng lặp hoặc log.
Skill xác nhận đúng hướng: đọc đúng brand kit `royce-shop`, tạo một cover AI mới, chọn ảnh dataset đúng ngành, rồi đăng bằng `fb_page_album`. Mình đang kiểm tra dữ liệu vòng trước để tránh đăng trùng khóa học liên tiếp và tìm entrypoint hub/plugin đã có trong vault.
Vòng gần nhất đã đăng **Tin học văn phòng & Ứng dụng AI**, nên vòng này mình sẽ loại khóa đó. Mình đang dùng lại cơ chế `scratch/hub_call.py` đã từng đăng thành công vì nó truyền đúng ngữ cảnh vault cho plugin Facebook.
Khóa được chọn cho vòng này là **Thiết kế đồ họa**, hợp lệ vì không trùng vòng trước. Mình sẽ tạo một poster GPT Image mới, sau đó chọn ảnh thật trong folder `do-hoa` để đủ album 5 ảnh.
Tool trực tiếp đã được nạp: `javis_generate_image` và `fb_page_album`. Mình sẽ gọi GPT Image trước với `ai_render_brand=true`, lưu ảnh vào `_xuat`, rồi kiểm nhanh ảnh trước khi đăng.
Poster mới đã tạo xong. Mình sẽ kiểm bằng mắt các điều kiện lớn: đúng brand, đúng tên khóa, không có “Royce Shop”, hotline/footer không sai rõ ràng, rồi mới ghép album.
Poster đạt các điều kiện chính: có logo/brand Sao Việt, đúng khóa **Thiết kế đồ họa**, hotline đúng, không có chữ Royce Shop. Mình sẽ chuẩn hóa cover và 4 ảnh thật ngành đồ họa thành album 5 ảnh trong `album_ready`.
Lệnh chuẩn hóa album đang chạy bằng helper cũ `hub_call.py pick_photos`, nó sẽ chọn ảnh chưa dùng nếu đọc được cache và xuất ảnh sang `attachments/dataset/_xuat/album_ready`. Mình chờ kết quả để kiểm đúng ngành trước khi đăng.
Album đã đủ 5 ảnh: cover AI mới và 4 ảnh gốc từ `do-hoa`. Mình sẽ tạo contact sheet tạm để kiểm bằng mắt rằng ảnh phụ không lạc ngành trước khi gọi Facebook.
Contact sheet ổn: ảnh phụ đều là lớp/phòng máy thiết kế, không lẫn kế toán hay AutoCAD. Mình sẽ ghi caption và payload vào `scratch/`, kiểm ký tự cấm, rồi đăng đúng một lần bằng `fb_page_album`.
Mình sẽ tạo 2 file mới trong `scratch/`: một file caption để lưu nội dung đã đăng, một file JSON payload để lần gọi Facebook có đầu vào rõ ràng và kiểm lại được.
Payload hợp lệ, 5 ảnh tồn tại và không có ký tự em dash trong file mình vừa ghi. Mình sẽ gọi `fb_page_album` đúng một lần cho bài đăng thật này.
Gọi trực tiếp bị chặn do tool không resolve được vault path trong kênh này, giống lỗi vòng trước. Chưa có bài nào ra Facebook ở lần lỗi đó, nên mình chuyển sang `scratch/hub_call.py` với `X-Javis-Vault`, cách đã xác minh hoạt động.
Mình đã đưa caption trực tiếp vào JSON payload để `hub_call.py` gọi được `fb_page_album` với đủ `message` và `photos`. Bây giờ sẽ đăng qua hub, chỉ tính là xong khi Facebook trả `post_id` thật.
Facebook đã trả `post_id` thật. Mình sẽ kiểm lại permalink qua Facebook rồi cập nhật `Javis/loop-state.json` để vòng sau tránh đăng trùng **Thiết kế đồ họa** liên tiếp.
Bài đã xác minh công khai với permalink thật. Mình sẽ ghi state vòng lặp và thử cập nhật danh sách ảnh đã dùng để vòng sau ưu tiên ảnh mới, nếu quyền file cho phép.
Lệnh ghi `_anh-da-dung.md` đang thử qua helper hiện có. Nếu vẫn bị quyền `root:root` chặn, mình sẽ không ép sửa quyền mà chỉ báo rõ hạn chế.
`_anh-da-dung.md` vẫn bị chặn quyền ghi, nên mình không cập nhật được cache ảnh đã dùng. Mình sẽ cập nhật `Javis/loop-state.json`, phần này thuộc state vòng lặp và đang ghi được.
State đã ghi khóa và post mới. Mình sẽ kiểm JSON hợp lệ và cập nhật trạng thái ngắn để bản ghi không còn chỉ phản ánh vòng trước.
Mình đã cập nhật `last_status` và `runs_today` cho vòng này. Bây giờ mình kiểm lại JSON, payload, và các file ảnh đã dùng trước khi kết thúc.
Đã đăng thật **1 bài Thiết kế đồ họa** lên Fanpage **Royce Shop**.

`POST_OK post_id=988656934325292_122131945647221350`  
Link: https://www.facebook.com/122131661457221350/posts/122131945647221350

Ảnh cover đã tạo:  
![poster Thiết kế đồ họa](attachments/dataset/_xuat/javis-img-20260907-173835-752e0b.png)

Đã chạm file:
- `attachments/dataset/_xuat/javis-img-20260907-173835-752e0b.png`
- `attachments/dataset/_xuat/album_ready/fb_norm_00.jpg` đến `fb_norm_04.jpg`
- `scratch/royce_do_hoa_caption_20260908.txt`
- `scratch/royce_do_hoa_album_20260908.json`
- `Javis/loop-state.json`

Đã xác minh bài công khai qua Facebook. Không cập nhật được `wiki/brand-kits/_anh-da-dung.md` vì file vẫn bị `PermissionError` do quyền `root:root`.

**Kiểm chứng:** ✓ Đạt: Post_id/permalink xác minh thật; payload/state/ảnh khớp Royce Shop và khóa Thiết kế đồ họa, JSON hợp lệ, không thấy hỏng file hay vượt phạm vi rõ ràng.
